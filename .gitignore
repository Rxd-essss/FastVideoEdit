# Generated artifacts
/out/
/cache/
/work/
# Auto-backup created by the first PUT /api/fillers (UI dictionary save)
/fillers_ru.yaml.bak

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Test media
/tests/_media/

# Verification / browser scratch
/.playwright-mcp/
/editor-*.png

# OS
Thumbs.db
.DS_Store

# Internal working docs (not for the public repo)
/COMPETITIVE_ANALYSIS.md
/TECH_UPGRADE_PLAN.md
/benchmark_results.md
/benchmark_results.json
/PILOTS_2026.md
/CLIP_MAKER_PLAN.md
/HANDOFF.md
/GLING_PLUS_PLAN.md
/REMAINING.md
/claude_code_video_pipeline_prompt.md

# User's own media / private session screenshots in the repo ROOT (never publish).
# Public README screenshots live in docs/ — the rule below is anchored to the
# root ("/"), so docs/*.png stays tracked.
/*.mp4
/*.png

# Large optional binaries — downloaded by the user (see README → Third-party)
/tools/*.exe
